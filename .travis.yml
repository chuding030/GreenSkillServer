language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

# clean 
script: "./gradlew clean build"