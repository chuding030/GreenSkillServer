language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 cache 활성화
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

# clean 후  build - build 시 자동으로 test 수행
script: "./gradlew clean build"